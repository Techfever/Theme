<?php if(count($this->pages) > 0){ ?>

<ul id="breadcrumb">
<?php
foreach ($this->pages as $key => $page) {
					?>					
	<li><?php
	if ($key < count($this->pages) - 1) {
	?><a href="<?php echo $page->getHref(); ?>"><?php echo $this->translate($page->getLabel()); ?></a><?php
		} else {
			echo $this->translate($page->getLabel());
		}
	?></li>
	<?php } ?>
	
</ul><?php } ?>